{
  "name": "@fixnote/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local MCP server over the FixNote notes database (stdio).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "bundle": "node scripts/bundle.mjs",
    "build:sea": "node scripts/bundle.mjs && node scripts/sea.mjs"
  },
  "dependencies": {
    "@fixnote/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.30.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "esbuild": "^0.28.2",
    "postject": "1.0.0-alpha.6",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
